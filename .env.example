PORT=4000
JWT_SECRET=replace-with-a-long-random-string

# Optional — Azure Speech for phoneme-level pronunciation assessment.
# Create a free Speech resource (F0 tier) at portal.azure.com and paste here.
# Leave blank to use the app's free pronunciation mode instead.
VITE_AZURE_SPEECH_KEY=
VITE_AZURE_SPEECH_REGION=eastus
